<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>

<body>
    <div class="w-full h-full bg-zinc-900 p=10 text-white">
        <a class="text-blue-500 " href="/">back to home</a>
        <h1 class="text-3xl mt-3 tracking-tighter  mb-3">All Users</h1>
        <div class="users flex  gap-3 flex-wrap">
            <%if(users.length> 0 ) { %>
                <% users.forEach((val)=>{ %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-lg overflow-hidden">
                        <div class="w-full h-72 rounded-lg bg-zinc-500">
                            <img class="w-full h-full object-cover object-top" src="<%= val.image%>" alt="we will add">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter">
                            <%= val.name %>
                        </h3>
                        <h5 class="Email text-zinc-500">
                            <%= val.email %>
                        </h5>
                        <div class="flex gap-3 mt-10">
                            <a class="text-zinc-3   00" href="/edit/<%=val._id%>">Edit User</a>
                            <a class="text-red-500" href="/delete/<%= val._id%>">Delete User</a>
                        </div>
                    </div>
                    <% }) %>

                        <% } else { %>
                            <h1 class="text-zinc-500 " style="font-size:xx-large">
                                No user yet...
                            </h1>
                            <% } %>



        </div>
    </div>
</body>

</html>